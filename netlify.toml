[build]
  publish = "."

# Redirects are defined in _redirects (root). Keeping them out of netlify.toml
# so the catch-all rule doesn't shadow the /.well-known/* passthrough.

[build.environment]
  NODE_VERSION = "18"
